name: Release

on:
  push:
    tags:
      - 'v*'
      - '[0-9]*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    env:
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
      ANDROID_DEFAULT_SERVER_URL: ${{ secrets.ANDROID_DEFAULT_SERVER_URL }}
      ANDROID_RELEASE_CERT_SHA256: f06cc88920b5f0cdc55c5ddcd51694fcf0131431900dbb24e8bd8820d965d4d4

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Require stable tag from main
        if: github.event_name == 'push'
        shell: pwsh
        run: |
          if ($env:GITHUB_REF_NAME -notmatch '^v?\d+\.\d+\.\d+$') {
            throw "Official releases require a stable semantic version tag, received $env:GITHUB_REF_NAME."
          }
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          git merge-base --is-ancestor $env:GITHUB_SHA origin/main
          if ($LASTEXITCODE -ne 0) {
            throw "Official release tag $env:GITHUB_REF_NAME must point to a commit on main."
          }

      - name: Require signed candidate from current main
        if: github.event_name == 'workflow_dispatch'
        shell: pwsh
        run: |
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          $mainSha = (git rev-parse origin/main).Trim()
          if ($LASTEXITCODE -ne 0 -or $env:GITHUB_SHA -ne $mainSha) {
            throw "Signed candidate builds must use the current origin/main commit."
          }

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Audit dependency security
        run: npm run verify:dependency-security

      - name: Verify release metadata
        id: release-metadata
        shell: pwsh
        run: |
          $packageVersion = [string](Get-Content package.json -Raw | ConvertFrom-Json).version
          if ($packageVersion -notmatch '^\d+\.\d+\.\d+$') {
            throw "Release package version must be stable semantic versioning, received $packageVersion."
          }
          if ($env:GITHUB_EVENT_NAME -eq 'push') {
            $tagVersion = $env:GITHUB_REF_NAME.TrimStart('v')
            if ($tagVersion -ne $packageVersion) {
              throw "Release tag $env:GITHUB_REF_NAME does not match package version $packageVersion."
            }
            $artifactVersion = $env:GITHUB_REF_NAME
          } else {
            $tagVersion = $packageVersion
            $artifactVersion = "v$packageVersion-candidate-$env:GITHUB_RUN_NUMBER"
          }

          $releaseNotesPath = "docs/release-notes-$tagVersion.zh-CN.md"
          if (-not (Test-Path -LiteralPath $releaseNotesPath)) {
            throw "Missing release notes: $releaseNotesPath"
          }
          "release_notes_path=$releaseNotesPath" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "artifact_version=$artifactVersion" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Run release verification
        run: npm run verify:release -- -RequireCleanGit -SchemaAudit skip

      - name: Package release bundle
        run: npm run package:release -- -Version "${{ steps.release-metadata.outputs.artifact_version }}" -OutputDir "${env:RUNNER_TEMP}\release"

      - name: Set up Java 24 for Android
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '24'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android build packages
        shell: pwsh
        run: |
          sdkmanager --install "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Require Android release signing secrets
        shell: pwsh
        run: |
          $requiredSecrets = [ordered]@{
            ANDROID_KEYSTORE_BASE64 = $env:ANDROID_KEYSTORE_BASE64
            ANDROID_KEYSTORE_PASSWORD = $env:ANDROID_KEYSTORE_PASSWORD
            ANDROID_KEY_ALIAS = $env:ANDROID_KEY_ALIAS
            ANDROID_KEY_PASSWORD = $env:ANDROID_KEY_PASSWORD
          }
          $missingSecrets = @(
            $requiredSecrets.GetEnumerator() |
              Where-Object { [string]::IsNullOrWhiteSpace([string]$_.Value) } |
              ForEach-Object { $_.Key }
          )
          if ($missingSecrets.Count -gt 0) {
            throw "Official Android releases require stable signing secrets. Missing: $($missingSecrets -join ', ')"
          }

      - name: Prepare Android signing files
        shell: pwsh
        run: |
          $keystoreDir = Join-Path $env:RUNNER_TEMP 'android-signing'
          New-Item -ItemType Directory -Force -Path $keystoreDir | Out-Null
          $keystorePath = Join-Path $keystoreDir 'cx-codex-release.jks'
          $keystorePropertiesPath = $keystorePath.Replace('\', '/')
          [System.IO.File]::WriteAllBytes($keystorePath, [Convert]::FromBase64String($env:ANDROID_KEYSTORE_BASE64))
          @"
          storeFile=$keystorePropertiesPath
          storePassword=$env:ANDROID_KEYSTORE_PASSWORD
          keyAlias=$env:ANDROID_KEY_ALIAS
          keyPassword=$env:ANDROID_KEY_PASSWORD
          "@ | Set-Content -Path "android/keystore.properties" -Encoding UTF8

      - name: Sync Android shell
        shell: pwsh
        env:
          CAP_SERVER_URL: ${{ env.ANDROID_DEFAULT_SERVER_URL }}
        run: npm run mobile:android:sync

      - name: Build Android release APK
        shell: pwsh
        env:
          APP_VERSION_NAME: ${{ steps.release-metadata.outputs.artifact_version }}
        run: |
          Push-Location android
          .\gradlew.bat assembleRelease
          Pop-Location

      - name: Verify Android release signature
        shell: pwsh
        run: |
          $apkPath = "android/app/build/outputs/apk/release/app-release.apk"
          $sdkRoot = if (-not [string]::IsNullOrWhiteSpace($env:ANDROID_SDK_ROOT)) {
            $env:ANDROID_SDK_ROOT
          } else {
            $env:ANDROID_HOME
          }
          $apksignerPath = Join-Path $sdkRoot "build-tools/36.0.0/apksigner.bat"
          if (-not (Test-Path -LiteralPath $apksignerPath -PathType Leaf)) {
            throw "apksigner was not found: $apksignerPath"
          }

          $certificateOutput = (& $apksignerPath verify --print-certs $apkPath 2>&1 | Out-String)
          if ($LASTEXITCODE -ne 0) {
            throw "Android release APK signature verification failed."
          }
          $digestMatch = [regex]::Match(
            $certificateOutput,
            "Signer #1 certificate SHA-256 digest:\s*(?<digest>[0-9a-fA-F]{64})"
          )
          if (-not $digestMatch.Success) {
            throw "Android release certificate SHA-256 digest was not found."
          }
          $actualDigest = $digestMatch.Groups["digest"].Value.ToLowerInvariant()
          $expectedDigest = $env:ANDROID_RELEASE_CERT_SHA256.ToLowerInvariant()
          if ($actualDigest -ne $expectedDigest) {
            throw "Android release certificate changed unexpectedly. Expected $expectedDigest but found $actualDigest."
          }
          Write-Host "Android release signature verified: $actualDigest"

      - name: Collect Android release artifacts
        shell: pwsh
        run: |
          $outputDir = Join-Path $env:RUNNER_TEMP 'release'
          $apkSource = "android/app/build/outputs/apk/release/app-release.apk"
          $artifactVersion = "${{ steps.release-metadata.outputs.artifact_version }}"
          $apkTarget = Join-Path $outputDir "cx-codex-android-$artifactVersion.apk"
          Copy-Item $apkSource $apkTarget -Force
          $hash = (Get-FileHash -Algorithm SHA256 $apkTarget).Hash.ToLowerInvariant()
          Set-Content -Path ($apkTarget + '.sha256') -Value "$hash  $(Split-Path -Leaf $apkTarget)" -Encoding ASCII

      - name: Verify release artifact checksums
        run: npm run verify:release-artifacts -- -OutputDir "${env:RUNNER_TEMP}\release"

      - name: Upload signed candidate artifacts
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: signed-release-candidate-${{ steps.release-metadata.outputs.artifact_version }}
          path: ${{ runner.temp }}/release/*
          retention-days: 3
          if-no-files-found: error

      - name: Publish GitHub release
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v3
        with:
          body_path: ${{ steps.release-metadata.outputs.release_notes_path }}
          files: |
            ${{ runner.temp }}/release/*.zip
            ${{ runner.temp }}/release/*.sha256
            ${{ runner.temp }}/release/*.apk
