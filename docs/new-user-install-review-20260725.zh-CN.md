# Windows 新人安装实测与改进建议（2026-07-25）

## 结论

从没有 CX-Codex 运行环境的 Windows 状态出发，按 README 的一行命令已经能够完成安装、构建、启动、免费临时公网访问、密码登录、发送消息和接收回复。

首次源码预览安装最终成功用时 153.5 秒。正式 2.5.6 已完成公开资产与安装回归，并补上仅限本机的二维码配对页。正式 2.5.7 修复了外层 PowerShell 捕获安装 JSON 时调用脚本不退出的问题；后续公开资产回归又发现安装器可能在公网验证仍为 `verifying` 时提前返回 `ok=false`，正式 2.5.8 已改为等待运行时真正 `ready`。正式 2.6.1 进一步兼容缺少 `Get-FileHash` 的 Windows PowerShell，并为 cloudflared 下载加入校验不降级的自动重试；最新公开 Release 已从全空状态一步安装并完成真实公网聊天复测。项目仍不能称为真正“傻瓜式”，主要差距变为：下载阶段缺少百分比、首次工作区选择不够明确，以及手机扫码后仍需手动输入密码。

## 2026-07-27：v2.6.0 完全卸载后的新人复测

本轮先通过官方 `-RemoveUserData -RemoveCloudflared -JsonOutput` 命令清除程序目录、用户状态、启动器、管理中心快捷方式、受管 cloudflared、7420 进程、任务和专属防火墙规则；再次确认源码仓库、`%USERPROFILE%\.codex` 登录态、工作区和 Android 正式签名不在删除范围。

首次直接执行 GitHub README 的正式 Release 安装命令，在 180.5 秒后复现阻断：内层 Windows PowerShell 校验 cloudflared 时无法使用 `Get-FileHash`。安装目录已回滚，但用户目录残留了一个约 54 MB 的 `.download-*` 半包。修复后，bootstrap 和安装器都会在缺少 `Get-FileHash` 时使用 .NET SHA-256，并通过刻意禁用该命令的独立测试；失败下载也由 `finally` 清理。

第二次从全空状态执行修复候选，在 110.8 秒后遇到 GitHub 大文件下载 EOF。原逻辑需要新人重新运行整条命令。安装器现改为最多自动重试三次，每次重试前删除半包并显示当前重试次数；不增加镜像、代理或关闭校验等高风险分支。

第三次完整安装成功用时 189.1 秒，安装结果 `ok=true`、`started=true`、`healthReady=true` 且无告警。随后验证：

| 验证项 | 结果 |
| --- | --- |
| 本机 `/health` 与 `/codex-api/health` | 200 |
| 本机管理中心 | 200；手机宽度无横向溢出，密码默认遮蔽 |
| 公网首页与 `/health` | 200 |
| 独立外部读取节点访问公网 `/health` | 200 |
| 未登录公网 API / WebSocket | 401 / 拒绝升级 |
| 公网 `/local-setup` | 404 |
| 密码登录、登录后 API、已登录 WebSocket | 200 / 200 / 101 |
| HTTPS Session Cookie | `Secure` |
| 管理中心生成新密码 | 200；旧 Cookie 和旧密码立即 401，新密码恢复 200 |
| 公网真实消息发送 | 202 `pending_start` |
| 消息最终状态 | 11.37 秒内 `completed` |
| 消息接收 | 收到 24 个 WebSocket 消息帧，线程回读和手机宽度前端均看到目标回复 |

这两项修复只有进入新的正式 Release 后，README 默认的“下载最新正式 Release”路径才会实际受益；仅更新 raw bootstrap 不能替换 v2.6.0 归档内的旧安装器。

## 2026-07-27：v2.6.1 正式公开资产复测

v2.6.1 发布后，再次使用该版本公开的官方卸载脚本清空 `%LOCALAPPDATA%\CX-Codex`、`%USERPROFILE%\.cx-codex`、启动器、管理中心入口和受管 cloudflared。复核确认 7420 监听、CX-Codex 任务、防火墙规则和受管运行进程均为零，同时源码仓库、`%USERPROFILE%\.codex`、用户工作区和 Android 签名密钥仍然存在。

随后只执行 README 面向新人的公开一行命令，不使用本地源码或本地归档。bootstrap 从 GitHub Release 解析并校验到 v2.6.1，正式首次安装用时 166.6 秒，最终返回 `ok=true`、`started=true`、`healthReady=true`、`phase=ready`、三项公网验证全为 `true`，告警数为零。安装目录、启动器和 7420 Node 进程均指向 `%LOCALAPPDATA%\CX-Codex`，没有回退到开发仓库。

| 正式 v2.6.1 验证项 | 结果 |
| --- | --- |
| GitHub Release ZIP / APK | 工作流发布成功；本地重新下载后 SHA-256 均匹配 |
| 安装来源与版本 | 公开 README 命令；GitHub Release v2.6.1 |
| 全新安装耗时 | 166.6 秒 |
| 本机健康与隧道状态 | `/health` 200；`ready` |
| 公网首页与 `/health` | 200 / 200 |
| 独立外部读取节点访问公网 `/health` | 200，内容确认 `status=ok` |
| 未登录公网 API / WebSocket | 401 / 401 |
| 公网 `/local-setup` | 404 |
| 密码登录、登录后 API、已登录 WebSocket | 200 / 200 / 101 |
| HTTPS Session Cookie | `Secure` |
| 公网真实消息发送 | 202 `pending_start` |
| 消息完成与接收 | 13.4 秒 `completed`；收到 28 个 WebSocket 帧；线程回读包含目标回复 |
| 393×852 管理中心 | 密码默认遮蔽、外网入口可见、无横向溢出 |
| 393×852 公网页面 | 登录页可见；登录后进入会话；目标回复可见；无横向溢出 |

本轮文档仍不记录临时公网地址、访问密码、Cookie 或会话 ID。临时地址可从本机管理中心查看，服务停止后会失效。

## 测试边界

清理并确认不存在：

- `%LOCALAPPDATA%\CX-Codex`
- `%USERPROFILE%\.cx-codex`
- `%USERPROFILE%\.local\bin\cx-codex-start.cmd`
- CX-Codex 安装的 `cloudflared*.exe`
- 7420 监听进程和健康端点
- 旧的 `FRP frpc 7420` 定时任务及其 7420 专属配置

保留：

- 源码仓库
- `%USERPROFILE%\.codex` 登录态
- Android 签名材料
- 用户工作区
- 不属于 7420 映射的 FRP 程序文件

## 新人实际步骤

1. 在普通 PowerShell 中执行 README 命令：

   ```powershell
   & ([scriptblock]::Create((irm 'https://raw.githubusercontent.com/Qjzn/CX-Codex/main/scripts/bootstrap-windows.ps1'))) -RemoteQuick -JsonOutput
   ```

2. 等待源码下载、依赖安装、前端/CLI 构建和 cloudflared 下载。
3. 本机打开 `http://127.0.0.1:7420/local-setup`，用手机相机扫描本机生成的二维码。
4. 手机打开临时 HTTPS 地址并输入配对页显示的密码；二维码本身不包含密码。
5. 浏览器中新建或选择工作区，本次选择 `CodexWorkspace`。
6. 发送一条测试消息并等待回复。
7. 不再需要手机入口时，在设置的“手机访问”卡片中停止临时地址；退出 CX-Codex 后地址也会失效。

文档和提交记录中不保存本次临时公网地址、访问密码、Cookie 或私人会话内容。

## 最终验证结果

| 项目 | 结果 |
| --- | --- |
| 本机 `/health` | 200 |
| Quick Tunnel 状态 | `ready` |
| 公网健康验证 | 通过 |
| 未登录公网 API | 401 |
| 未登录 WebSocket | 拒绝连接 |
| 公网 `/local-setup` | 404 |
| 密码登录 | 200 |
| 登录后 API | 200 |
| HTTPS Session Cookie | `Secure` |
| 浏览器发送 | 已发送 |
| 浏览器接收 | 收到“新人安装验证成功” |
| 实时连接 | 正常 |
| 正式 Release 首次安装 | 2.5.3 建立基线；最新 2.6.1 公开资产一步安装并完成公网真实聊天 |
| 运行中升级与同版本重复升级 | 2.5.3 通过 |
| 默认保留数据卸载与重装 | 2.5.3 通过；2.5.4 正式版关闭超时误警告 |
| 正式五进程无警告卸载 | 2.5.4 通过 |
| Quick Tunnel 瞬时不可达恢复 | 正式 2.5.5 自动重试一次 |
| 本机二维码配对 | 正式 2.5.6 已通过真实解码和公网 404 验证 |
| PowerShell 捕获安装 JSON | 正式 2.5.7 已在外层变量捕获、真实服务健康、三项公网鉴权和官方卸载链路中通过 |
| 安装结果等待公网就绪 | 正式 2.5.8 已从公开 Release 安装到 `ready` 后返回，三项公网验证全部通过 |
| Windows PowerShell 哈希兼容 | 正式 2.6.1 在缺少 `Get-FileHash` 时使用 .NET SHA-256，校验规则不降级 |
| cloudflared 下载中断恢复 | 正式 2.6.1 最多自动重试三次，每次先清理半包再重新下载和校验 |

## 实测发现并已经修复

1. 某些 Windows `npm.cmd --version` 会同时输出代码页提示和版本号，旧逻辑把多行数组直接转换为版本对象而失败。现在只提取合法语义版本行。
2. npm 启动脚本会受全局 prefix 影响，即使使用便携 Node.js，也可能跳转到旧 NVM 目录中的 npm 6。现在由选定的 `node.exe` 直接执行随包 `npm-cli.js`。
3. 显式 Node.js 路径接入后，启动器、登录检查和 watchdog 仍引用旧变量，曾生成以 `""` 开头的无效启动命令。变量引用已统一，Windows CI 增加启动器内容断言。
4. 本机健康失败后仍等待 150 秒公网地址。现在本机启动失败会立即跳过隧道等待。
5. 全新安装失败只删除项目目录，曾残留运行状态、启动器和 cloudflared。现在只清理本次新建的文件，安装前已有内容不动。
6. GitHub Actions 的 PowerShell 冷启动偶发超过 5 秒。探测超时调整为 15 秒，减少无业务原因的 CI 波动。
7. `-JsonOutput` 原先会混入构建输出。现在 stdout 只保留一行版本化 JSON，进度/诊断进入 stderr；失败返回固定错误码和阶段。
8. raw bootstrap 与旧 Release 可能错配。现在归档携带 `release-capabilities.json`，bootstrap 会在组合新参数前验证能力。
9. 缺少官方卸载入口。现在默认卸载保留用户数据、Codex 登录态、工作区和 Android 签名，可显式清运行数据与项目托管的 cloudflared。
10. `StartNow` 使用通用日志时可能被已有实例占用。现在使用端口级启动日志，并用真实健康与卸载关停作为 Windows CI 门禁。
11. 正式 2.5.1 首次安装成功后，重复执行原命令会因旧服务占用安装目录而无法原子替换。2.5.2 已停止目标父服务与隧道，但正式回归又发现 `codex.exe` 子进程可能继续持有目录；2.5.3 改为停止整棵受管进程树、有限重试，失败时自动恢复旧服务。
12. 安装 JSON 被外层 PowerShell 变量或自动化管道捕获时，常驻 Node 服务可能继承原生进程作业或输出句柄，导致 JSON 已产生但调用脚本不退出。2.5.7 改由 CIM 创建隐藏 worker，再由 worker 启动带独立日志句柄的服务；回归要求外层捕获退出后服务继续健康，并可由官方卸载完整停止。
13. 正式 2.5.7 公开资产首次安装中，本机健康已通过、Quick Tunnel 几秒后也完全可用，但安装器在运行时仍为 `verifying` 时读取了一次状态并返回 `ok=false`。正式 2.5.8 会持续等待 `active + ready + health + HTTP auth + WebSocket auth`，并给瞬时公网失败重试保留约 4 秒窗口；同一条 README 命令的公开资产复测已通过。
14. Windows PowerShell 环境可能无法解析 `Get-FileHash`，旧安装器因此在 cloudflared 下载完成后阻断并留下半包。正式 2.6.1 使用 .NET SHA-256 作为等价后备，并在所有路径通过 `finally` 清理临时下载。
15. GitHub 大文件下载偶发 EOF 时，旧逻辑要求新人重新执行整条命令。正式 2.6.1 会自动重试最多三次，仍坚持固定 Release URL 和 SHA-256 校验，不引入不受控镜像或跳过校验。

## 槽点

### 已关闭：正式发布一致性

- 2.5.1 正式 Release 已完成下载、SHA-256、首次安装、本机健康和公网鉴权回归。
- 2.5.3 正式 Release 已补齐运行中重复升级、默认保留数据卸载和无安装状态重装；Windows CI 会让旧服务及其子进程锁定目录后再次执行 bootstrap。
- 2.5.4 正式版让官方卸载器以子进程优先顺序关闭整棵受管进程树，真实五进程卸载与自动化夹具均无超时警告。
- 正式 2.5.5 在 Quick Tunnel 公网验证完全不可达时自动重试一次，但错误鉴权状态仍立即失败。
- 正式 2.5.6 已提供仅限本机的二维码配对页；正式 2.5.7 已关闭 PowerShell 捕获安装结果时的后台服务句柄挂起。
- 2.5.7 发布后公开安装实测发现并复现了 `verifying` 提前返回；正式 2.5.8 已在同一台机器以外层变量捕获方式通过真正的 ready 边界。
- 正式 2.6.1 已从全空状态通过公开 README 命令完成安装、外网鉴权、真实消息收发和手机宽度 UI 验证，关闭 `Get-FileHash` 缺失与 cloudflared EOF 两类新人阻断。
- README 默认使用 Release + SHA-256 链路，不再依赖 `main` 源码归档。
- Release 工作流强制校验标签、`package.json` 和版本说明一致，避免误发错版本。

### P1：安装体验

- 冷安装约 2.5–3 分钟，cloudflared 下载期间已有存活提示，但没有下载百分比或剩余时间。
- npm/Vite 输出较多，新人难以判断“正常构建”还是“安装失败”。
- 已有最多三次自动重试，但仍没有下载百分比、剩余时间、代理/镜像配置、断点续传或明确的重试按钮。
- 安装后虽然生成 `CodexWorkspace`，已有 Codex 项目的用户仍可能默认停留在上次项目，需要手动切换。

### P1：日常使用

- 临时地址会变化，无 SLA，退出进程后失效。
- 安全模式默认不创建自启动任务；重启电脑后需要重新启动并获得新地址。
- 仍需先在本机打开配对页并在手机输入密码；地址已可扫码打开，密码刻意不写入二维码或 URL。

### P2：可维护性

- Windows bootstrap、安装器、CLI 各自承担部分运行时和隧道逻辑；目前已有安装/运行中升级/启动/健康/卸载组合门禁，每个正式 Release 仍需保留一次公网下载回归作为发版证据。
- GitHub Dependabot 未启用，Code Scanning 没有分析结果。
- 前端构建存在大于 500 kB 的 chunk 警告，影响首次打开速度。

## 优化为“傻瓜式”的建议

### 第一阶段：发布即可用

1. 已完成：2.5.3 正式 Release 已完成公网下载、运行中升级、同版本重复升级、卸载和重装验证；2.5.4 正式版关闭卸载的父子进程与误警告边界，正式 2.5.5 收口临时公网传播抖动。
2. 已完成：bootstrap 读取同版本能力清单，确认归档支持 `RemoteQuick`、`JsonOutput` 和所需安装器契约，拒绝混用。
3. 已完成：stdout 最终只输出一行稳定 JSON；进度和诊断写入 stderr，失败提供固定错误码和阶段。
4. 已完成：增加官方 `uninstall-windows.ps1`，默认保留 Codex 登录态、工作区和 Android 签名，可选清除运行数据与托管 cloudflared。
5. 已完成：Windows CI 增加真正的 `StartNow` 外层变量捕获、健康和卸载关停检查。
6. 已完成：`RemoteQuick` 安装结果等待运行时完成公网健康、HTTP 鉴权和 WebSocket 鉴权，不再把短暂 `verifying` 状态当作最终失败。

### 第二阶段：首次引导

1. 安装完成自动打开本机引导页，按“Codex 登录 → 选择工作区 → 本机健康 → 手机访问”显示四步状态。
2. 部分完成：配对页已显示本地生成的二维码、地址和密码，二维码不含密码且公网无法打开配对页；后续再补有效期和停止按钮。
3. 首次消息提供“发送测试消息”按钮，自动验证发送、实时进度、回复和断线恢复。
4. 明确提供“安全临时访问”和“长期固定访问”两个模式，后者引导 Tailscale 或命名 Cloudflare Tunnel。

### 第三阶段：真正面向普通用户

1. 提供签名的 Windows 安装包，安装、更新、修复、启动和卸载统一到一个界面。
2. 增加下载缓存、校验后复用、失败重试、代理设置和可选镜像。
3. 使用当前用户级后台服务管理进程，不要求管理员权限；在界面中清晰控制开机启动。
4. 为安装器、服务、隧道和聊天发送/接收建立一条 Windows 端到端发布门禁。

## 建议的最终用户体验

理想情况下，新人只需要：

1. 下载并运行官方签名安装包。
2. 登录 Codex 或复用已有登录。
3. 选择一个工作区。
4. 扫描二维码。
5. 发送第一条消息。

其余 Node.js、npm、构建、端口、隧道、密码、升级和进程管理都应由安装器完成，并在失败时给出可复制的错误码和“一键修复”。
